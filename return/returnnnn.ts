import '../smallletter/i'
import '../smallletter/j'
import '../smallletter/k'
import '../smallletter/l'
let t5:any,t6:any
void function returnnnn(){
    void  [   'i / Math.atan(NaN + t5)' + 'j / Math.atan2(0 - t6)',
              'k / Math.atan2(NaN - t5)'  + 'l / Math.atan(0 + t6)',
              'k / Math.atan(0 + t5)' + 'j / Math.atan2(NaN -t6)',
              'k / Math.atan2(0 - t5)' + 'l / Math.atan(NaN - t6)'
           ]
    return [t5,t6]
}
